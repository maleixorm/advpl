#include 'protheus.ch'
#include 'parmtype.ch'

User Function RELACIONAL()
    Local nNum1 := 10
    Local nNum2 := 20

    Alert(nNum1 < nNum2)
    Alert(nNum1 > nNum2)
    Alert(nNum1 = nNum2)
    Alert(nNum1 == nNum2)
    Alert(nNum1 <= nNum2)
    Alert(nNum1 >= nNum2)
    Alert(nNum1 != nNum2)
Return
